{
  "name": "typescript-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "./buildfile",
    "clean": "rm -r build/*",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watch 'npm run build' lib/ web/"
  },
  "author": "Dan Schultz",
  "license": "ISC",
  "dependencies": {
    "react": "^0.14.7",
    "react-dom": "^0.14.7"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "tsify": "^0.13.2"
  }
}
